---
description: Laravel backend standards — apply when artisan + laravel/framework detected (Deduped split)
globs:
  - "artisan"
  - "composer.json"
  - "app/**/*.php"
  - "routes/**/*.php"
  - "database/**/*.php"
  - "bootstrap/**/*.php"
  - "config/**/*.php"
alwaysApply: false
---

# Laravel (detect: `artisan` + `composer.json` တွင် `laravel/framework`)

FastAPI = `05` · Node/Express = `10`။

- Surface `backend`/`fullstack`။ Routes/Controllers/Migrations/Eloquent = Laravel conventions။
- DB = Eloquent/Query Builder parameterized — raw concat SQL မဆောက်ရ။
- AuthZ = policies/gates/middleware။ Form/state-changing → CSRF (`@csrf`/VerifyCsrfToken)။
- Validation = Form Request / validator။ Secrets = `.env` + `config()` — hardcode မလုပ်ရ။
- Test = `phpunit` / `pest` ရှိရင် Testing Gate (`07`) အတိုင်း။
